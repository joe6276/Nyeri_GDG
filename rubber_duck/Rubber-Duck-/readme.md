Step 1: Replace the API_KEy with your own API Key

Step2: run `npm install` to install node_modules

step 3: install parcel bundler :`npm install -g parcel-bundler`

step 4 ; Run the project :`parcel index.html`